<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:maui="clr-namespace:epj.RadialDial.Maui;assembly=epj.RadialDial.Maui"
             x:Class="RadialDialSample.MainPage">

  <VerticalStackLayout
    VerticalOptions="Center"
    Spacing="20">

    <maui:RadialDial
      x:Name="RadialControlControl"
      VerticalOptions="Center"
      WidthRequest="250"
      HeightRequest="250"
      TouchInputEnabled="True"
      StrokeWidth="{Binding Path=Value, Source={x:Reference StrokeWidthSlider}}"
      DialColor="Orange"
      BaseColor="LightSkyBlue"
      Min="0"
      Max="60"
      ShowScale="True"
      InternalPadding="10"
      ScaleDistance="10"
      ScaleLength="24"
      ScaleThickness="8"
      ScaleUnits="5"/>

    <Label 
      HorizontalOptions="Fill"
      FontSize="20"
      HeightRequest="50"
      HorizontalTextAlignment="Center"
      VerticalTextAlignment="Center"
      Text="{Binding Path=Value, Source={x:Reference RadialControlControl}}"/>

    <Slider 
      x:Name="StrokeWidthSlider"
      Minimum="1"
      Maximum="300"
      Value="45"/>
  </VerticalStackLayout>

</ContentPage>
